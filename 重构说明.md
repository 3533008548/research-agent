# 🔄 重构说明 — LangGraph + RAG 集成

> 日期: 2026-07-15  
> 重构目标: 引入 LangGraph 图编排 + ChromaDB RAG 论文检索  
> 重构范围: 拆分为 5 模块，Agent 核心由 `while` 循环升级为 `StateGraph` 状态机

---

## 一、从单文件到模块化

### 重构前（v1）

```
research_agent.py  (600+ 行，单文件)
├── 搜索函数 (search_arxiv, search_semantic_scholar)
├── read_pdf      (下载 + PyMuPDF → 后来 pdfplumber)
├── list_downloaded_papers
├── ResearchAgent 类
│   ├── _register_builtin_tools
│   ├── _call_api
│   └── step()  →  while max_turns 循环
└── CLI main()
```

### 重构后（v2）

```
research_agent/                    # 5 个模块，各司其职
│
├── tools.py                       # 工具函数（无依赖）
│   ├── search_arxiv()
│   ├── search_semantic_scholar()
│   └── list_downloaded_papers()
│
├── pdf_reader.py                  # PDF 增强阅读器（已有，不变）
│   ├── PaperReader 类
│   └── read_pdf_enhanced()
│
├── paper_store.py   ← NEW         # RAG 论文向量存储
│   ├── chunk_text()               # 分块策略
│   └── PaperStore 类
│       ├── index_paper()          # 论文 → 向量
│       ├── query()                # 语义检索
│       ├── list_papers()          # 已索引列表
│       └── delete_paper()         # 删除
│
├── graph_builder.py  ← NEW        # LangGraph 推理引擎
│   ├── AgentState (TypedDict)
│   ├── llm_node()                 # LLM 调用节点
│   ├── tool_node()                # 工具执行节点
│   ├── router()                   # 条件路由
│   └── build_graph()              # 图构建工厂函数
│
├── research_agent.py              # CLI 入口（轻量包装）
│   ├── ResearchAgent 类           # 包装 LangGraph app
│   └── CLI main()
│
└── requirements.txt               # 新增 3 个依赖
```

---

## 二、详细变动

### 2.1 `tools.py` — 工具函数独立

**为什么改**: 工具函数被 `graph_builder.py` 和 CLI（`/papers` 命令）两边引用，放在独立模块避免循环导入。

| 原位置 | 新位置 |
|--------|--------|
| `research_agent.py:search_arxiv()` | `tools.py:search_arxiv()` — 代码不变 |
| `research_agent.py:search_semantic_scholar()` | `tools.py:search_semantic_scholar()` — 代码不变 |
| `research_agent.py:list_downloaded_papers()` | `tools.py:list_downloaded_papers()` — 代码不变 |

```python
# 导入方式
from tools import search_arxiv, search_semantic_scholar, list_downloaded_papers
```

---

### 2.2 `paper_store.py` — RAG 论文向量库

**为什么加**: 解决「论文 35000 字只能喂前 8000 字」的瓶颈。论文全文分块存向量库，查询时只检索相关段落。

**技术选型**:

| 选择 | 理由 |
|------|------|
| ChromaDB (PersistentClient) | 持久化到磁盘，重启后论文库不丢失 |
| DefaultEmbeddingFunction (ChromaDB 内置 ONNX) | all-MiniLM-L6-v2（384维/~80MB），首次自动下载，之后本地缓存。**无需安装 sentence-transformers/PyTorch** |
| 余弦距离 (cosine) | ChromaDB 默认，语义相似度度量 |

**分块策略** (`chunk_text`):

```
论文全文
  ├─ 按 "\n\n" 分段
  ├─ 每块 ≤ 600 字符
  ├─ 长段落用滑动窗口 (重叠 100 字符)
  └─ 保留 char_start/char_end 元数据用于定位
```

**存储结构**:

```
chroma_data/
└── collection "papers"
    ├── embedding: [0.012, -0.034, ...]  (384维)
    ├── document:  "In this section we describe..."  (块文本)
    ├── id:        "paper_xxx_chunk_5"
    └── metadata:  {paper_id, title, chunk_index, char_start, char_end}
```

**公开 API**:

```python
store = PaperStore(persist_dir="./chroma_data")

# 索引论文（约 2-5 秒/篇）
paper_id = store.index_paper(text, title="Attention Is All You Need")

# 检索（< 100ms）
results = store.query("What is the loss function?", top_k=3)
# → [{text, title, paper_id, distance, chunk_index, char_start}, ...]

# 管理
store.list_papers()       # 已索引论文列表
store.delete_paper(pid)   # 删除一篇
```

**与 Agent 的交互**: `read_pdf` 工具执行后自动调用 `paper_store.index_paper()`。用户随后用 `query_papers` 在已索引论文中检索。

---

### 2.3 `graph_builder.py` — LangGraph 推理引擎

**为什么加**: 替换 `while max_turns` 循环，获得：
- 对话持久化（MemorySaver → 重启不丢对话）
- 状态机语义（每个节点职责明确，可单独测试）
- 预留中断/恢复接口（未来 Human-in-the-Loop）

**图结构**:

```
                   ┌───────────────────────────┐
                   │     AgentState            │
                   │  messages: list[dict]     │  ← Annotated[list, add] 自动追加
                   │  metadata: dict           │
                   └───────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │      START        │
                    └────────┬──────────┘
                             │
                    ┌────────▼──────────┐
                    │    llm_node()     │  ──→ DeepSeek API + 5个 tool schema
                    │  (LLM 推理)        │
                    └────────┬──────────┘
                             │
                    ┌────────▼──────────┐
                    │    router()       │
                    │  (条件路由)        │
                    └───┬──────────┬────┘
                        │          │
              tool_calls? yes    no
                        │          │
              ┌─────────▼───┐  ┌──▼───┐
              │ tool_node() │  │ END  │
              │ (执行工具)    │  └──────┘
              └─────┬───────┘
                    │
                    └────→ llm_node()  (循环)
```

**与旧版 while 循环的对照**:

| 旧版 | 新版 (LangGraph) |
|------|------------------|
| `for turn in range(max_turns):` | `ConditionalEdge("llm", router, ...)` |
| `if msg.get("tool_calls"):` | `router()` 函数 |
| `func = self._tool_functions[name]` | `_execute_tool()` 集中分发 |
| `self._messages.append(...)` | `return {"messages": [...]}` → 自动追加 |
| 无持久化 | `MemorySaver()` → 跨重启恢复 |

**新增工具**:

| 工具 | 功能 |
|------|------|
| `query_papers(query, top_k)` | RAG 语义检索已索引论文 |
| `list_indexed_papers()` | 查看 RAG 库中的论文列表 |

**关键设计决策**:

1. **PaperStore 实例通过闭包注入**: `build_graph(..., paper_store=store)`。因为 State 需要 JSON 序列化（Checkpoint 要求），不能把 Python 对象放 State 里。
2. **工具执行集中在 `_execute_tool()`**: 一个函数做所有工具分发，按需导入 `tools.py`/`pdf_reader.py`，避免循环依赖。
3. **PDF 阅读后自动索引**: `_execute_tool("read_pdf")` 在提取文本后自动调用 `paper_store.index_paper()`，用户无需手动操作。

---

### 2.4 `research_agent.py` — 轻量 CLI 包装

**变化**: 从 600+ 行缩减到 ~200 行。核心工作全部委托给 `graph_builder.build_graph()`。

```python
class ResearchAgent:
    def __init__(self):
        self.paper_store = PaperStore()           # RAG
        self._app = build_graph(                  # LangGraph
            api_key=..., paper_store=self.paper_store
        )
        self._config = {"configurable": {"thread_id": "session-xxx"}}

    def step(self, user_input):
        state = {"messages": [{"role": "user", "content": user_input}]}
        result = self._app.invoke(state, config=self._config)
        return result["messages"][-1]["content"]

    def reset(self):
        self._config = {"thread_id": f"session-{uuid}"}  # 新会话
```

**新增 CLI 命令**:

| 命令 | 功能 |
|------|------|
| `/indexed` | 查看 RAG 库中已索引的论文 |
| `/model` | 现在同时显示 RAG 状态 |

---

### 2.5 `requirements.txt`

```diff
  requests>=2.28.0
  pdfplumber>=0.10.0
+ chromadb>=0.5,<1.0
+ langgraph>=0.2
  python-dotenv>=1.0.0
```

---

## 三、架构演进对比

```
          v1 (单文件)              v2 (模块化)
          ──────────              ──────────
          
Agent 核心  while 循环            StateGraph 状态机
对话持久化  内存丢失               MemorySaver (SQLite)
论文阅读    截断 8000 字           全文索引 + RAG 检索
工具注册    ResearchAgent._tools   graph_builder._build_tool_schemas()
代码量      1 file, 600+ 行        5 files, ~200-400 行/文件
扩展性      耦合                    工具模块独立
```

---

## 四、兼容性说明

### 保持不变
- `read_pdf()` 的 Agent 调用接口完全不变
- `search_papers()` 参数和返回格式不变
- `data/papers/` 缓存目录不变
- CLI 命令 `/help` `/new` `/papers` `/quit` 不变
- `.env` 加载逻辑不变

### 新增
- RAG 数据目录: `chroma_data/`
- 3 个新模块: `tools.py` `paper_store.py` `graph_builder.py`
- 2 个新工具: `query_papers` `list_indexed_papers`
- 1 个新 CLI 命令: `/indexed`

### 删除
- `ResearchAgent._register_builtin_tools()` → `graph_builder._build_tool_schemas()`
- `ResearchAgent._call_api()` → `graph_builder.llm_node()`
- `ResearchAgent._search_papers_wrapper()` → `graph_builder._execute_tool()`
- `research_agent.py` 中的 `read_pdf()` 函数 → `graph_builder._execute_tool()` 内部
- `research_agent.py` 中的搜索函数 → `tools.py`

---

## 五、学习参考

本次重构应用了你 `deepseek` 项目中以下技术：

| 技术 | 源文件 | 应用位置 |
|------|--------|----------|
| StateGraph + Node + ConditionalEdge | `langgraph_agent.py` | `graph_builder.py:build_graph()` |
| MemorySaver 持久化 | `human_in_loop.py` | `graph_builder.py:MemorySaver()` |
| ChromaDB PersistentClient | `rag_demo.py` | `paper_store.py:PaperStore.__init__()` |
| ChromaDB DefaultEmbeddingFunction (ONNX) | `rag_light.py` | `paper_store.py:PaperStore.__init__()` |
| 闭包注入共享资源 | `unified_demo.py` | `graph_builder.py:build_graph(paper_store=...)` |

---

*本文档记录于 2026-07-15，对应 `需求决策日志.md` 条目 004，更新于条目 006。*

---

## 六、条目 006 更新 — 系统提示词重构 + SqliteSaver 持久化

### 6.1 `_SYSTEM_PROMPT` — 本地优先策略

**问题**：Agent 无法区分"本地已有论文"和"需要全网搜索"，每次都从头搜索，忽略已索引论文。

**修改**（`graph_builder.py`）：

```diff
- ## 核心能力
- 1. 论文搜索 — 用 search_papers 工具搜索相关论文
- 2. PDF阅读 — 用 read_pdf 工具下载并阅读论文全文
- 3. 论文检索 — 用 query_papers 工具在已索引论文中精准检索

+ ## ⚠️ 信息获取优先级（必须严格遵守）
+ 1. 本地优先 — 先用 query_papers 检索本地论文库
+ 2. 外部补充 — 本地不足时才用 search_papers
+ 3. 精读后检索 — read_pdf 后追问优先用 query_papers
+ 4. 明确告知来源 — 区分「已读论文」和「新搜索结果」
```

**效果**：Agent 的行为从"每次都像新生开学"变成"知道自己读过什么"。

### 6.2 `SqliteSaver` — 对话持久化

**问题**：`MemorySaver` 只存在内存中，关闭终端后对话历史全部丢失。

**修改**：

| 文件 | 变更 |
|------|------|
| `graph_builder.py` | `MemorySaver()` → `SqliteSaver.from_conn_string(checkpoint_db)` |
| | 新增 `checkpoint_db` 参数（默认 `"checkpoint.db"`） |
| `research_agent.py` | `_new_config()` 移除，改为固定 `thread_id = "research-main"` |
| | `/new` 命令生成新 UUID 作为 thread_id，旧对话保留在 DB |
| | 启动时检测 `checkpoint.db` 存在则打印提示 |
| `.gitignore` | 新增 `checkpoint.db` 排除 |

**效果**：

```
重启前：
  >>> 帮我搜索 TSN 调度论文
  ... (读了3篇，讨论了很多)
  >>> /quit

重启后：
  💾 对话历史: checkpoint.db (156 KB)
  >>> 继续上次的分析    ← Agent 记得之前读过的3篇论文和讨论内容
```

### 6.3 改动文件清单

| 文件 | 改动行数 | 说明 |
|------|---------|------|
| `graph_builder.py` | ~30 行 | 系统提示词重写 + SqliteSaver 替换 |
| `research_agent.py` | ~25 行 | 固定 thread_id + 启动检测 + 帮助文本 |
| `.gitignore` | +1 行 | 排除 checkpoint.db |

---

## 七、条目 007 更新 — 公式识别修复（阶段一）

### 7.1 问题根因

公式丢失是三环节叠加致死，不是单个 bug：

```
论文 PDF (40,000 字符, Method 部分含 15 个核心公式)
     │
  环节 A: pdfplumber 对 LaTeX 公式先天性失明（∫/∑/∂ → 乱码）
     │
  环节 D: max_chars=8000 一刀切 → 85% 公式被整体丢弃
     │
  环节 F: query_papers 返回 r['text'][:300] → chunk 后半被切
     │
  LLM 看到：前 8000 字的摘要+引言，无任何完整公式
```

### 7.2 修复（零风险，零新依赖）

| 修复项 | 文件 | 原值 | 新值 |
|--------|------|------|------|
| read_pdf 截断 | `graph_builder.py:410` | `max_chars=8000` | `max_chars=50000` |
| query_papers 截断 | `graph_builder.py:444` | `r['text'][:300]` | `r['text']` |
| chunk_size | `paper_store.py:42` | `600` | `1000` |
| chunk overlap | `paper_store.py:43` | `100` | `150` |
| 路径 fallback | `graph_builder.py:404` | 精确路径 | 自动尝试 `data/papers/` |
| 默认 max_chars | `pdf_reader.py:84,383` | `8000` | `50000` |

**核心逻辑变更**：
- `read_pdf` 不再负责"喂给 LLM"，只负责"喂给 ChromaDB"
- LLM 对话通过 tool_node 的 12000 字符截断兜底
- 用户追问细节 → `query_papers` → ChromaDB 返回完整 chunk
- 路径 fallback 解决 `list_papers` 能列出但 `read_pdf` 找不到的 bug

### 7.3 效果

```
修复前：
  user: "公式(10)是什么"
  → query_papers → chunk 命中但[:300]截断 → "损失函数定义为..."
  → LLM: "我无法识别公式(10)的具体数学表达式"

修复后：
  user: "公式(10)是什么"  
  → query_papers → ChromaDB 返回完整 chunk:
    "The final loss combines: L_total = L_task + λL_cons + γL_div (10)"
  → LLM: "公式(10)是 L_total = L_task + λL_cons + γL_div"
```

### 7.4 改动文件清单

| 文件 | 改动行数 | 说明 |
|------|---------|------|
| `graph_builder.py` | ~6 行 | max_chars 8000→50000 + [:300]移除 + 路径 fallback |
| `paper_store.py` | ~2 行 | chunk_size 600→1000 + overlap 100→150 |
| `pdf_reader.py` | ~2 行 | 默认 max_chars 8000→50000 |

---

## 八、条目 008 — Web UI

### 8.1 设计原则

极简聊天界面，使用 Gradio ChatInterface。开箱即获得：
- LaTeX 公式 MathJax 自动渲染（**解决终端公式显示问题**）
- 浏览器内多轮对话
- 侧栏显示模型 / Token / RAG 状态

### 8.2 新增文件

| 文件 | 说明 |
|------|------|
| `web_ui.py` | Gradio ChatInterface 封装 ResearchAgent |
| `requirements.txt` | 新增 `gradio>=5.0` |

### 8.3 使用

```powershell
python web_ui.py                      # 默认 deepseek-chat, 端口 7860
python web_ui.py -m deepseek-v4-pro   # 指定模型
python web_ui.py --port 8080           # 自定义端口
```

浏览器打开 `http://localhost:7860`，公式 `$$...$$` 自动渲染。

### 8.4 代码结构

```
web_ui.py (~50 行逻辑)
├── ResearchAgent 实例化 (复用 CLI 的所有能力)
├── chat_fn → agent.step()
├── status_text → token/模型/RAG 统计
└── gr.Blocks → 极简 CSS + ChatInterface
```

终端版本 (`research_agent.py`) 保留不受影响，两者共用同一套后端。

---

## 九、条目 009 更新 — Web UI 完善 + 论文清理 + 公式渲染修复

### 9.1 Web UI 命令系统

在 `chat_fn` 中拦截斜杠命令，直接返回 Markdown 不走 LLM：

| 命令 | 功能 |
|------|------|
| `/help` | 命令列表 |
| `/model` | 模型 + Token + RAG 状态表格 |
| `/tokens` | Token 消耗表格（含费用） |
| `/indexed` | 已索引论文列表 |
| `/new` | 新对话 |

### 9.2 状态栏实时刷新

改用 `chatbot.change` 事件在每次对话后自动更新状态栏，解决 Token 始终显示零的问题。

### 9.3 论文清理工具

**`graph_builder.py`**:
- 新增 `delete_paper(paper_id_or_title)` 工具 → 模糊匹配标题或 ID 后删除
- `read_pdf` 索引前自动检测同名论文 → 先删旧索引再存新索引（去重）

### 9.4 公式渲染修复

初版正则 `(?<!\$)\$([^\$]+?)\$(?!\$)` 转换所有 `$...$`，导致单字母 `$g$` → `$$g$$` 后文字排列错乱。

修复为仅转换含 LaTeX 特征的内容：
```
正则: (?<!\$)\$([^\$]*[\\_^][^\$]*)\$(?!\$)
含义: 匹配 $ 内包含反斜杠、下划线、或上标符的内容

$g$           → $g$          (单字母，不动)
$a_t$         → $$a_t$$      (下标，转换)
$\nabla$      → $$\nabla$$   (LaTeX，转换)
```

### 9.5 Gradio 6.0 适配清单

| 旧 API | 新写法 |
|--------|--------|
| `gr.Blocks(theme=, css=)` | `demo.launch(theme=, css=)` |
| `retry_btn=, undo_btn=, clear_btn=` | 移除（Gradio 6 已废弃） |
| `demo.load(fn, every=5)` | `chatbot.change(fn)` 事件 |

### 9.6 改动文件清单

| 文件 | 说明 |
|------|------|
| `web_ui.py` | 命令拦截 + 状态刷新 + 智能公式正则 + Gradio 6 API 适配 |
| `graph_builder.py` | delete_paper 工具 + read_pdf 自动去重 |
| `requirements.txt` | 新增 gradio>=5.0 |

---

## 十、条目 010 更新 — 笔记功能

### 10.1 数据模型

SQLite 双表结构（`notes.db`）：

```sql
topics (id, name UNIQUE, created_at)
notes  (id, topic_id, content, created_at, FOREIGN KEY topic_id)
```

### 10.2 交互方式

**聊天框命令**：
```
/note topics          列出所有话题
/note topic TSN调度    切换话题
/note add 发现L2 loss... 添加笔记（支持长文本）
/note list            列出当前话题笔记
/note del 3           删除第3条
```

**Web UI 弹出面板**（Accordion "📝 笔记"，默认折叠）：
- 话题下拉框（支持自定义新话题）
- 多行文本输入区
- 保存按钮
- 笔记列表（时间倒序）

### 10.3 新增文件

| 文件 | 说明 |
|------|------|
| `notes.py` | NoteStore 类（SQLite CRUD + 话题管理 + 搜索） |
| `web_ui.py` | 新增 /note 命令 + 笔记面板（~100 行增量） |
| `.gitignore` | 排除 notes.db 及 WAL 缓存 |

零新依赖 — `notes.py` 仅用 Python 标准库 `sqlite3`。

---

## 十一、条目 011 — PDF 图片提取 + 多模态

### 11.1 方案演进

| 尝试 | 结果 |
|------|------|
| Marker (marker-pdf 2.0) | Python 3.14 无预编译 Pillow wheel → 编译失败 |
| | tokenizers 版本锁死 → 降级 0.22.2 |
| | HF 镜像 401 + 子进程不继承代理 → 模型下不完 |
| **最终方案** | PyMuPDF 图片提取 + GLM-4V API 看图 |

### 11.2 实现

`pdf_reader.py` 新增 `extract_images()` — PyMuPDF 逐页提取嵌入图片到 `data/papers/images/`。

`graph_builder.py` 新增 `describe_image` 工具 — base64 编码图片 → GLM-4V API → 中文描述。路径自动 fallback。

### 11.3 删除

| 删除 | 原因 |
|------|------|
| `MarkerReader` 类 | 模型下不完 |
| `marker-pdf` 包 | 已 pip uninstall |
| `parser` 参数 | read_pdf 简化回单一解析器 |

---

## 十二、条目 012 — Prompt Engineering 系统级升级

### 12.1 智能上下文压缩

`verify_node` 中检测 `read_pdf` 后 LLM 生成摘要卡片 → 将 3000+ 字符原文替换为一行引用，读 3 篇论文回收 ~30K tokens。

### 12.2 少样本范例（Few-shot）

`_SYSTEM_PROMPT` 末尾新增 2 个范例：
- 范例1：精读论文后的摘要卡片格式（`[已读]` 标签用法）
- 范例2：回答论文细节的来源标注（`[已读·Table 2]` 格式）

### 12.3 跨论文库自检

`verify_node` 中检测 `read_pdf` → 调用 `query_papers` 检索 2 条最相关内容注入验证 prompt，LLM 交叉对比数据和来源。

### 12.4 上下文估算修复

`research_agent.py` 新增 `_load_context_from_checkpoint()` — 启动时从 `checkpoint.db` 读取已有消息估算 tokens，状态栏不再显示 0%。

### 12.5 改动文件

| 文件 | 改动 |
|------|------|
| `graph_builder.py` | `_SYSTEM_PROMPT` 加范例 + `verify_node` 重写（压缩+自检） |
| `research_agent.py` | `_load_context_from_checkpoint()` 启动估算 |

---

## 十三、条目 013-014 — 用户画像 + 双记忆 + 分析

详见 `需求决策日志.md` 条目 013（三角度分析）、条目 014（双记忆架构）。

---

## 十四、条目 015-016 — 多 Tab + 测试 + verify 修复

### 四 Tab 布局

`web_ui.py` 用 `gr.Tabs` 分为：💬 对话 / 📚 论文库 / 📝 笔记 / ⚙ 设置。

### 配置文件

`config.yaml`（YAML）统一非敏感设置，PyYAML 读写。Web UI 设置面板可修改并保存。

### 核心测试

`tests/test_core.py` — 13 个 unittest，覆盖 read_pdf/query_papers/verify 三条链路、验证状态清理和每日检索重试，~4s 跑完。

### verify 修复

反馈从 `messages` 改走 `metadata.verify_feedback`，聊天框不可见。工具结果截断 600→2000，公式检查宽松。

---

## 十五、条目 016-017 — 拆分 + 日志 + 降级 + 配置

### 15.1 拆分 graph_builder

从 930 行单文件拆为：
- `prompts.py` — 系统提示词
- `tool_schemas.py` — 8 个工具 schema
- `tools/` — 4 个工具模块（search / read_pdf / describe / profile_tool）
- `graph_builder.py` — 仅图结构（280 行）

旧 `tools.py` 重命名为 `search_api.py`（arXiv/Semantic Scholar API 函数）。

### 15.2 日志

`logger.py` — 统一日志配置，`get_logger("ra.xxx")` 使用。`--debug` 开关 DEBUG 级别。

### 15.3 降级

`paper_store.NoOpStore` — ChromaDB 不可用时自动接管，Agent 仍可对话。

### 15.4 配置统一

`config.py` Config 类 — 合并 `.env` + `config.yaml` + 环境变量 + CLI 参数。优先级：CLI > env > yaml > 默认值。

---

## 十六、条目 018 — 多模态文件输入 + 校验

### 16.1 文件输入

`web_ui.py` 对话 Tab：
- `gr.MultimodalTextbox` — 输入框 📎 附件按钮
- 拖拽覆盖层 — 拖文件时显示 "📥 释放文件以上传"，松手自动保存
- PDF → 自动注入 `read_pdf` 命令
- 图片 → 自动注入 `describe_image` 命令

### 16.2 输入校验

| 校验 | 触发条件 | 行为 |
|------|---------|------|
| 大小 | >100MB | 拒绝 |
| 同名 | 文件名已存在 | 询问覆盖/重命名 |
| 同内容 | MD5 一致 | 询问保存/跳过 |

---

---

*本文档记录于 2026-07-15，更新于条目 006-025。*

## 十七、条目 023-024 — 切块 / 图片 / verify / token

详见 `需求决策日志.md` 条目 023（分层切块 + 图注图片提取 + verify 分级）、024（token 优化）。

---

## 十八、条目 025 — 稳定性修复与发布前回归

### 18.1 CLI 与统一配置

- `research_agent.py` 启动日志改为读取 `cfg.model`，避免 API Key 配置正确后仍因未定义变量退出。
- `/model-set <name>` 通过 `dataclasses.replace(agent.cfg, model=name)` 重建 Agent，保留密钥、RAG 和数据目录等配置。
- `web_ui.py` 的 `-m` 默认值改为 `None`；没有显式命令行覆盖时，Web UI 会正确使用 `config.yaml` 和环境变量。

### 18.2 Web UI 文件和每日检索

- 后台每日检索队列在线程启动前创建，并由互斥锁避免自动检索与手动重试并发执行。
- 文件重名/重复内容的“覆盖、重命名、保存、跳过”确认流程会读取确认消息，并恢复首次上传时的原始说明。
- 新增 `Scheduler.search()`（不落库的临时检索）和 `Scheduler.retry_today()`（清除当日记录后重试）；`/daily search` 和 `/daily retry` 现在执行真实操作。

### 18.3 验证状态和 RAG 降级

- `verify_feedback`、重试次数和问题历史仅在当前修正链路中保留；通过、跳过、API 失败或达到重试上限后均会清理。
- 达到验证上限时追加带风险提示的最终消息，而不再直接修改已有 state。
- ChromaDB 导入和 embedding 函数放进同一 `try` 分支，使 `NoOpStore` 能在缺失 RAG 依赖时正确降级。

### 18.4 回归测试

新增两项无外网测试：

1. 模拟 Tool Calling 和 verify 响应，确认成功验证后旧反馈不会进入下一轮会话。
2. 使用临时 SQLite 数据库，确认每日检索可重试且临时检索不依赖外部 API。

测试中的 LangGraph checkpoint 使用内存 SQLite，`Scheduler` 增加 `close()` 以避免 Windows 临时数据库文件锁。

---

## 十九、条目 026 — 多会话隔离与会话管理

### 19.1 状态边界

旧实现把 `research-main`、`token_usage` 和 `_stream_cb` 放在 `ResearchAgent` 实例级别，多个会话共用一个实例时会出现历史、统计或流式 token 串线。现在改为：

- `session_store.py` 在 `checkpoint.db` 中维护 `agent_sessions` 和 `agent_session_usage`；
- LangGraph 的 `thread_id` 等于会话 ID，节点从 `metadata.session_id` 读取记忆摘要；
- 每次 `step()` 创建请求专属的编译图、Token 字典和流式回调；
- 同一会话使用 `RLock` 串行化聊天、读取历史和删除操作；不同会话没有共享回调。

### 19.2 Web UI 行为

对话页新增会话下拉框、新建按钮、删除确认复选框和删除按钮。浏览器侧使用 `gr.State` 保存当前会话 ID；切换时从 checkpoint 恢复用户/助手消息，首条消息自动取前 20 个字符作为标题。

### 19.3 删除范围与测试

删除会话会清除所有具有 `thread_id` 列的 LangGraph checkpoint 表行，以及该会话的摘要和 Token 统计；全局论文库、笔记、PDF、三元组和图片描述缓存保留。`tests/test_core.py` 增加旧会话迁移、硬删除隔离和按 session 读取摘要测试；加上长任务限时与 verify 熔断测试后，核心回归测试共 17 项。

---

## 二十、条目 027 — 长任务与外部服务降级

### 20.1 每日检索不再阻塞聊天

`/daily search` 和 `/daily retry` 仅负责入队，立即释放 Gradio 聊天事件。后台线程使用独立 `Scheduler` SQLite 连接执行任务，页面顶部任务面板通过 `gr.Timer` 轮询进度和结果，因此结果不会混入任何会话的聊天历史。

检索层将 Semantic Scholar、arXiv、OpenAlex 改为 3 个并行请求，去掉人为串行等待；每个源使用 `(3.05 秒连接, 8 秒读取)` 超时。一个源超时只会记入诊断信息，不会阻塞另外两个源返回结果。

### 20.2 verify 快速失败与熔断

verify 是辅助质量链路，不应决定用户是否能收到主回答。`CircuitBreaker` 连续记录两次失败后，在 120 秒恢复窗口内跳过 verify；调用成功会清零失败计数。状态栏明确显示“验证暂时跳过”，避免把可选检查的网络故障误认为主回答失败。

---

## 二十一、条目 028 — 主模型同模型重试与背压

`llm_client.py` 集中管理主模型 HTTP 调用。它使用全局信号量限制并发，同时限制等待队列，避免大量浏览器请求无限堆积。对于 429、5xx、连接和读取超时，客户端只重试当前配置的模型，并使用指数退避和随机抖动；4xx 参数/鉴权错误不重试。

每个请求存在总截止时间，连续失败会打开主模型熔断器并快速反馈给用户。流式响应在首 token 前中断时可安全重放；一旦输出任何 token，系统只保留已输出内容并提示 `/retry`，不擅自拼接另一段模型回答。整个过程不含备用模型或自动降级逻辑。

---

## 二十二、条目 029 — 端到端预算与半开熔断

首版“请求截止时间”只覆盖拿到并发槽后的 HTTP 首包；排队和 SSE 正文可能额外占用等待时间，且 HTTP 200 会过早清除失败计数。现将一次模型调用抽象为共享 `RequestBudget`：从进入队列开始计时，HTTP 重试和首 token 前的 SSE 重放共用最多两次重试额度，流式读取也持续检查同一截止时间。

对于流式响应，模型客户端在收到 HTTP 头后继续持有并发槽，直到读到 `[DONE]` 或中断并关闭连接；只有完整结束才记录熔断成功。`CircuitBreaker` 改为 `closed → open → half_open` 三态，恢复期后仅放行一个探测请求，探测失败立即重新打开，避免下游刚恢复时被并发流量再次压垮。

新增回归测试覆盖：排队占用截止时间、流式连接持有并发槽且仅完成后清零失败计数，以及半开状态只允许单探测请求。

---

## 二十三、条目 030 — 统一运行时数据边界

新增 `runtime_paths.py`，以 `APP_DATA_DIR`（默认 `./runtime`）为全部可变数据的唯一入口。`primary/` 保存 SQLite、PDF、画像和用户设置；`derived/` 保存可重新构建的 Chroma 索引和 PDF 图片；`meta/state.json` 记录数据结构版本。应用启动只创建缺失目录与版本文件，不会自动移动旧版根目录数据。

`Config` 统一向 Agent、Web UI、RAG 和工具模块注入这些路径。Web UI 的模型、RAG、页数和每日检索开关改存 `primary/settings.json`，不再运行时改写版本控制下的 `config.yaml`。上传文件名会规范化到受控目录，避免路径穿越。

提供 `scripts/migrate_runtime.py`（默认 dry-run，`--apply` 才复制）和 `scripts/backup_runtime.py`（通过 SQLite online backup 输出 ZIP）。迁移保留旧数据，且 `derived/` 可按需忽略备份并重新生成。
## 二十四、条目 031 — Docker Compose 单服务部署

### 目标

让项目可通过 Docker Desktop 一条命令启动，同时将代码、密钥和用户运行数据隔离。

### 实现

- 新增 `Dockerfile`，以 Python 3.11-slim 构建，安装项目依赖后以非 root 用户运行；
- 新增 `docker-compose.yml`，将宿主机 `./runtime` 映射到容器 `/app/runtime`，并通过 `.env` 注入 API Key；
- 新增健康检查、优雅停止时间、`init` 进程和自动重启策略；
- 新增 `.dockerignore` 与 `.env.example`，防止密钥、运行数据、数据库和 Git 历史进入镜像；
- Web UI 新增 `--host` 参数，默认仅监听本机，容器命令显式使用 `0.0.0.0`。
- 容器用户显式使用 `/home/app` 及其缓存目录，避免系统用户默认的 `/nonexistent` 导致 Chroma/ONNX 写缓存时报 `PermissionError`。

### 数据边界

容器可以随时删除和重建；`runtime/` 才是唯一需要持久化的卷。`docker compose down` 不会删除该目录；只有显式执行带卷删除的命令或手动删除 `runtime/` 才会影响用户数据。
## 二十五、条目 032 — 多会话前端事件隔离

### 问题

“新建会话”同时更新 Chatbot、下拉选择器和 `gr.State`。Chatbot 的程序化 `change` 事件又会将读取到的旧会话状态写回下拉选择器，形成会话回跳、历史串线和事件排队。

### 修复

- Chatbot 变更事件只更新状态栏，不再回写会话下拉框；
- 新建和切换会话时同步后端默认 `thread_id`，作为 CLI 兼容状态；
- Web 聊天请求必须携带有效的浏览器级 `session_state`，缺失或失效时显式失败，禁止回退到其他会话。

---

## 二十六、条目 033 — 最小多 Agent 深度研究闭环

### 26.1 用户视角

对话输入框下新增来源选择、**🧭 深度研究** 和“继续上次研究”按钮；也可输入 `/research` 命令。页面展示规划、并行取证、综合、质检、修订等阶段，但不让用户承担内部 Agent 选择或参数配置。长时间等待只低频发送心跳，避免不断写入相同 Gradio 状态造成页面负担。

### 26.2 编排边界

新增 `research_orchestrator.py`，将深度研究限制在一次可审计的有限链路：

```text
Planner → Local Researcher ∥ Public Researcher → Synthesis → Critic → Revision (0/1)
```

两个研究员共用 `ResearchAgent` 的 `LLMClient`，因此仍受统一并发、排队、端到端截止时间、同模型重试和取消令牌控制。研究员有各自白名单工具、独立内存 checkpoint，并关闭常规 `verify` 节点；最终由集中 Critic 只检查一次，避免重复请求。

### 26.3 数据与历史

`session_store.py` 新增 `research_runs`，存放计划、证据、质检、报告和脱敏 trace，并以 `thread_id` 与当前会话绑定。会话删除和孤儿清理按既有“所有含 thread_id 的表”规则自然覆盖该表。聊天 checkpoint 仅追加用户的研究问题和最终报告，不保存研究员的 tool call 或中间消息。

停止或失败会保留已得到的证据；同一会话通过 `/research continue` 会跳过重新规划和研究员执行，直接从证据综合继续。若没有可用证据，则按新研究运行处理。

---

## 二十七、条目 034 — 每日检索多 Agent 化

### 27.1 边界与成本

每日检索不把每个数据源或每篇论文都变成一次模型请求。`daily_orchestrator.py` 中的 OpenAlex、OpenAIRE、DBLP Scout 是结构化、无模型的来源 Agent；它们并行运行但同一来源使用独立信号量，避免多个关键词同时触发限流。临时检索与深度研究公开文献则使用 OpenAlex 和 arXiv，arXiv 不参与每日推送。

Curator 接收已去重的整次候选集（最多 12 条），通过共享 `LLMClient` 只做一次批量排序；只有候选数量不足或质量门控发现元数据缺失时，才执行一次低优先级 Critic。主模型失败时保留规则排序和质量提示，不新增备用模型。

### 27.2 可恢复数据

`daily.db` 新增 `daily_runs`、`daily_candidates`、`daily_agent_events`：分别保存运行状态、统一候选对象与阶段事件。候选对象包含标题、摘要、作者、年份、来源、DOI、链接、引用量与命中关键词。`/daily resume` 读取失败/停止运行已保存的候选，从 Curator 开始继续，避免重新访问公开 API。

### 27.3 交互

后台面板现在显示当前 Agent 阶段、推荐理由、标签、质量提示与运行 ID；`/daily stop` 会同时关闭自动运行并向当前后台任务传递取消信号。结果仍停留在独立任务面板，不写入聊天会话。

---

## 二十八、条目 035 — 论文来源分层与 OpenAlex 鉴权

`Semantic Scholar` 因长期限流从代码和工具 Schema 中移除，`Europe PMC` 不纳入当前以通信网络与计算机研究为主的范围。每日推送固定为 `OpenAlex + OpenAIRE + DBLP`：OpenAlex 提供广覆盖与引文元数据，OpenAIRE 补充开放获取记录，DBLP 负责计算机领域的会议与期刊条目。Crossref 保留为未来的 DOI 元数据补全候选，不增加到首版抓取链路。

`/daily search` 与深度研究的公开文献工具仅使用 `OpenAlex + arXiv`；前者适合常规文献检索，后者覆盖预印本和新近研究，避免将“每日新发表”的排序偏好误用于深度调研。

配置键统一为 `.env` 中的 `OPENALEX_API_KEY=<raw key>`，不使用 `Bearer`。`Config` 将它传给每日 Scout，交互式工具从同一环境变量读取，并以 OpenAlex API 所需的 `api_key` 查询参数发送；该 Key 从不写入数据库、日志、页面或 Git。
