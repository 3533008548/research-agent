# 🧪 科研助手 Agent — 需求决策日志

> 记录每一次功能需求讨论、决策方向和技术演进。
> 日志格式：日期 | 需求/问题 | 决策方向 | 状态

---

## 条目 001 — 科研助手Agent初始构建

| 字段 | 内容 |
|------|------|
| **日期** | 2026-06-18（首轮对话） |
| **需求来源** | 用户希望做一个能帮助阅读论文、总结研究方向、辅助科研的Agent |
| **核心诉求** | 能自己找论文、能识别PDF、能总结研究方向 |
| **技术基础** | 基于 `D:\develop\learn\cli-chatbot` 的技术栈（Python + DeepSeek API + Function Calling） |
| **决策方向** | 复用用户已有的 Function Calling Agent 架构，新增三个工具：`search_papers`（arXiv/Semantic Scholar双数据源）、`read_pdf`（PyMuPDF下载解析）、`list_papers`（本地缓存管理）；单文件demo结构 |
| **交付物** | `research_agent.py`（25KB 单文件Agent）、`requirements.txt`、`data/papers/` 缓存目录 |
| **状态** | ✅ 已交付 |

### 讨论要点

- **搜索API选择**：决定双数据源 — Semantic Scholar（引用数+PDF链接，信息丰富）和 arXiv（覆盖面全，作为备选）
- **PDF解析方案**：使用 PyMuPDF (`fitz`) 做基础文本提取，15页/8000字符截断防token爆炸
- **缓存策略**：自动本地缓存已下载PDF，避免重复下载
- **系统提示词设计**：定义三层工作流 — 搜索阶段 → 精读阶段 → 分析阶段

### 三视角分析结论

| 视角 | 结论 |
|------|------|
| **用户最想要的功能** | 持久化论文库 + 笔记系统，Agent能"记住"研究成果 |
| **用户下一步该学** | RAG（检索增强生成）— 解决论文太长、信息检索困难的问题 |
| **现存最大技术问题** | PDF解析丢失核心结构（公式、表格、布局、图表），上游瓶颈锁死了Agent理解深度 |

---

## 条目 002 — 迁移至 research_agent/ 目录 + 创建需求日志

| 字段 | 内容 |
|------|------|
| **日期** | 2026-06-18 |
| **需求** | 将所有文件迁移到 `research_agent/` 子目录下，并创建需求决策日志（即本文档） |
| **决策方向** | 移动 `research_agent.py`、`requirements.txt`、`data/` 到子目录，创建 `.gitignore` 保护API Key和PDF缓存 |
| **状态** | ✅ 已交付 |

---

---

## 条目 003 — PDF 解析三大改进：表格感知 + 双栏布局 + 章节标注

| 字段 | 内容 |
|------|------|
| **日期** | 2026-06-18 |
| **需求** | 用 pdfplumber 替代 PyMuPDF，实现三项增强：①表格感知提取并格式化为 Markdown ②双栏布局自动识别并先左后右排序 ③章节标题自动检测并标注 ## |
| **决策方向** | 创建独立模块 `pdf_reader.py`，内含 `PaperReader` 类；`research_agent.py` 保留下载/缓存逻辑，提取部分委托给 `PaperReader`；依赖从 PyMuPDF 切换为 pdfplumber |
| **交付物** | `pdf_reader.py`（14KB，含 PaperReader 类 + 便捷函数 + 独立测试入口）、`research_agent.py`（read_pdf 函数重构）、`requirements.txt`（更新依赖） |
| **状态** | ✅ 已交付 |

### 技术细节

**1. 表格感知提取（`_extract_tables`）**
- 使用 `pdfplumber.page.extract_tables()` 提取结构化表格
- 自动清理空 cell、对齐列数、格式化为标准 Markdown 表格
- 输出插入在对应页面的文本之后，用「━━━ 第N页（表格）━━━」标注

**2. 双栏布局识别（`_detect_columns` + `_extract_text`）**
- 按 `(y, x)` 排序单词 → 分组为行 → 统计左/右/跨栏行的比例
- 判定条件：左栏 > 15% 且 右栏 > 15% 且 跨栏行 < 50%
- 输出顺序：跨栏标题 → 插入左栏对应 y 位置 → 全部右栏（用「─── 右栏 ───」分隔）

**3. 章节自动标注（`_mark_sections`）**
- 内置 30+ 常见学术章节名（Introduction / Related Work / Methodology / Experiments …）
- 支持编号前缀（`1.`、`2.1`、`I.`、`II.` …）
- 匹配行前加空行，标注 `## 标题`，标题后加空行
- 过滤过长行（>80字符）避免误匹配

### 架构说明

```
research_agent.py
  └─ read_pdf(url_or_path, max_pages)
       ├─ URL 解析 & 下载缓存（保持不变）
       └─ → read_pdf_enhanced()  ←  pdf_reader.py 模块
              └─ PaperReader.read()
                   ├─ _extract_text()     ← 双栏感知
                   ├─ _extract_tables()   ← 表格→Markdown
                   └─ _mark_sections()    ← ## 章节标注
```

---

---

## 条目 004 — LangGraph + RAG 集成

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-15 |
| **需求** | 用户学习了 LangGraph 和 RAG（ChromaDB + SentenceTransformer），希望将两者集成到 research_agent 中 |
| **核心诉求** | ① RAG 解决论文 35000 字只能喂前 8000 字的瓶颈 ② LangGraph 替换 while 循环，获得对话持久化和状态管理 |
| **决策方向** | RAG 立即加入（论文向量化 + 语义检索），LangGraph 重构 Agent 核心（StateGraph 替换 while 循环），PaperStore 实例通过闭包注入图节点 |
| **交付物** | `tools.py`（搜索函数独立）`paper_store.py`（RAG向量库）`graph_builder.py`（LangGraph图）`research_agent.py`（重构为轻量包装器）`重构说明.md`（详细变动文档）`requirements.txt`（新增3依赖） |
| **状态** | ✅ 已交付 |

### 技术要点

**RAG (paper_store.py)**
- ChromaDB PersistentClient + **ChromaDB 内置 ONNX DefaultEmbeddingFunction**（all-MiniLM-L6-v2, ~80MB，首次自动下载）
- **无需 sentence-transformers/PyTorch**：嵌入由 ChromaDB 的 ONNX Runtime 内部完成
- 分块策略：按 `\n\n` 分段 → 600字符/块 → 100字符重叠窗口
- PDF 阅读后自动索引：`read_pdf` → `paper_store.index_paper()` 全自动
- 新增工具 `query_papers` / `list_indexed_papers`

**LangGraph (graph_builder.py)**
- 图结构：`llm_node` ⇄ `tool_node` 循环，`router()` 条件路由
- MemorySaver 持久化对话（thread_id 隔离不同会话）
- 参考 `unified_demo.py` + `langgraph_agent.py` 的 ReAct 模式
- PaperStore 实例通过 `build_graph(paper_store=...)` 闭包注入

**架构变化**
- 从 600+ 行单文件 → 5 个模块（tools / pdf_reader / paper_store / graph_builder / research_agent）
- Agent 核心 while 循环 → LangGraph 状态机
- 新增 2 个依赖：chromadb（含内置 ONNX Embedding）, langgraph

### 用户学习成果的应用

| 技术 | deepseek 源文件 | research_agent 应用位置 |
|------|----------------|----------------------|
| StateGraph + ConditionalEdge | `langgraph_agent.py` | `graph_builder.py:build_graph()` |
| MemorySaver | `human_in_loop.py` | `graph_builder.py:MemorySaver()` |
| ChromaDB | `rag_demo.py` | `paper_store.py:PaperStore` |
| ChromaDB DefaultEmbeddingFunction (ONNX) | `rag_light.py` | `paper_store.py:PaperStore.__init__()` |
| 闭包注入 | `unified_demo.py` | `graph_builder.py:build_graph()` |

---

---

## 条目 005 — 多模型支持 + Token 监测 + 三角度分析

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-15 |
| **需求** | ① 支持 deepseek-chat / deepseek-v4-pro / flash 多模型切换 ② 监测 Token 消耗和费用 ③ 三角度分析当前项目现状 |
| **决策方向** | 模型通过 CLI 参数 (`-m`)、环境变量 (`DEEPSEEK_MODEL`)、运行时命令 (`/model-set`) 三级切换；Token 统计通过闭包可变 dict 由 graph_builder 写回 research_agent，每轮对话后自动展示消耗 |
| **交付物** | `research_agent.py`（CLI argparse、/model-set、/tokens、chat 中 delta 展示）、`graph_builder.py`（llm_node 中提取 usage）、`需求决策日志.md`（条目005） |
| **状态** | ✅ 已交付 |

### 技术要点

**模型切换**
- CLI: `python research_agent.py -m deepseek-v4-pro` 或 `--no-rag`
- 环境变量: `.env` 中 `DEEPSEEK_MODEL=flash`
- 运行时: `/model-set deepseek-reasoner` 重建 Agent，保留 RAG 论文库
- `/model` 命令增强：同时显示当前模型、token 累计、RAG 状态

**Token 监测**
- `graph_builder.py:llm_node` 从 DeepSeek API 响应中提取 `usage` 字段
- 通过闭包可变 dict 回写 `research_agent.py` 的 `agent.token_usage`
- 每轮 LLM 调用实时打印 `📊 tokens: +2347 (P:2000 C:347) 累计: 12890`
- 每轮对话后显示本轮增量和累计消耗
- `/tokens` 命令展示详细统计（调用次数、输入/输出 tokens、费用估算）
- 费用按 DeepSeek 官方定价（chat: $0.27/$1.10 /百万 token）

### 三角度分析结论

| 视角 | 结论 |
|------|------|
| **用户最缺** | 记忆连续性 — 重启后忘了研究方向、读过哪些论文、正在进行什么任务 |
| **该学什么** | SqliteSaver + Epilogue 模式 — 几十行代码实现跨 session 持久化 |
| **最大问题** | Agent 分不清本地知识和全网搜索 — 每次都"从头来"，不查已索引论文 |

---

---

## 条目 006 — 系统提示词重构 + SqliteSaver 持久化

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-15 |
| **需求** | 条目005三角度分析揭示的两个核心问题：① Agent 分不清本地知识和全网搜索 ② 重启后记忆丢失 |
| **决策方向** | ① 系统提示词增加「信息获取优先级」章节，强制先 query_papers 再 search_papers ② MemorySaver → SqliteSaver.from_conn_string("checkpoint.db") |
| **交付物** | `graph_builder.py`（提示词重构 + SqliteSaver）、`research_agent.py`（固定 thread_id + 启动检测）、`.gitignore`（排除 checkpoint.db）、`重构说明.md`（条目006更新） |
| **状态** | ✅ 已交付 |

### 技术要点

**系统提示词 — 本地优先策略**
- 新增 `## ⚠️ 信息获取优先级（必须严格遵守）` 章节
- 四级优先级：本地 query_papers → 外部 search_papers → 精读后 query_papers → 明确告知来源
- 工具顺序重排：本地检索提到论文搜索前面
- 新增行为约束："先查后搜：任何问题都先 query_papers → 不够再 search_papers"

**SqliteSaver — 对话持久化**
- `graph_builder.py`: `MemorySaver()` → `SqliteSaver.from_conn_string("checkpoint.db")`
- `research_agent.py`: 固定 `thread_id = "research-main"`，重启自动恢复对话
- `/new` 生成 UUID thread_id，旧对话保留在 DB 中可追溯
- 启动时检测 `checkpoint.db` 存在则显示大小提示
- `.gitignore` 排除 `checkpoint.db`

---

---

## 条目 007 — 公式识别问题诊断与修复方案

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-19 |
| **需求** | Agent 反馈无法识别论文中的公式(10)(11)，请求诊断根因并提出修复方案 |
| **决策方向** | 三阶段修复：阶段一（三个数字改动，零依赖）→ 阶段二（引入 Marker 公式感知提取）→ 阶段三搁置（Nougat/MathPix/MinerU） |
| **交付物** | `graph_builder.py`（max_chars 提升 + 移除 300 截断 + 路径修复）、`paper_store.py`（chunk_size 1000）、`pdf_reader.py`（默认 max_chars 提升） |
| **状态** | ✅ 已交付 |

### 根因分析

公式丢失是三个环节叠加：

| 环节 | 位置 | 丢失量 | 机制 |
|------|------|--------|------|
| D — 8000 字符截断 | `graph_builder.py:read_pdf` `max_chars=8000` | ~80% | Method/推导部分全部丢弃 |
| F — 检索结果 300 截断 | `graph_builder.py:query_papers` `r['text'][:300]` | ~10% | chunk 后半被切，恰好是公式位置 |
| A — pdfplumber 先天性失明 | `pdf_reader.py:_extract_text` | ~5-10% | LaTeX 公式符号无 Unicode 映射 |

叠加后：一篇论文约 85-95% 的公式对 LLM 不可见。另有附带 bug：`read_pdf` 路径解析与 `list_papers` 不一致。

### 修复（阶段一）

| 修复项 | 原值 | 新值 | 效果 |
|--------|------|------|------|
| read_pdf 截断 | `max_chars=8000` | `50000`（由 tool_node 的 12000 截断兜底） | ChromaDB 索引全文 |
| query_papers 截断 | `r['text'][:300]` | `r['text']`（完整） | LLM 看到完整 chunk |
| chunk_size | `600` | `1000` | 更完整公式段落 |
| 路径解析 | 只支持精确路径 | fallback 到 `data/papers/` | 匹配 list_papers 输出 |

### 调研结论

开源公式提取工具对比：Marker（GPLv3，96.67 分）和 MinerU（中文优化）是两大首选。Nougat 休眠。MathPix 商业。pymupdf4llm 不做公式。阶段一修复即可解决 ~80% 问题，阶段二 Marker 补全剩余 ~15%。

---

## 条目 008 — Web UI

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-19 |
| **需求** | 终端显示公式 `$$...$$` 为原始 LaTeX，不美观。需一个能自动渲染数学公式的界面 |
| **决策方向** | 选择 Gradio ChatInterface 做极简 Web UI（公式自动 MathJax 渲染），终端版本保留不变，两者共用同一套后端 |
| **交付物** | `web_ui.py`（~50行 Gradio 封装）、`requirements.txt`（新增 gradio>=5.0）、`重构说明.md`（条目008） |
| **状态** | ✅ 已交付 |

### 技术要点

- Gradio ChatInterface 内置 LaTeX 渲染：`$$...$$` → 数学公式排版
- 零侵入：`web_ui.py` 直接调用 `ResearchAgent.step()`，不修改后端
- 极简 CSS：纯白背景、无 footer、状态栏显示模型/Token/RAG
- CLI 参数：`-m` 模型、`--port` 端口、`--no-rag`

### 方案淘汰记录

| 方案 | 淘汰原因 |
|------|---------|
| A — 复制 LaTeX 到 Typora | 手动步骤多，用户体验差 |
| B — Unicode 近似转换 | 只支持简单公式，`\left[\right]` 无法处理 |
| C — `/export` HTML | 不如直接 Web UI 一步到位 |
| D — Web UI → **选中** | 改动量 50 行，公式零失真渲染 |

---

## 条目 009 — Web UI 完善 + 论文清理 + 公式渲染修复

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-19 |
| **需求** | ① Web UI 无法执行 /model /tokens 等命令、Token 始终显示零 ② RAG 中同一论文被重复索引 3 次 ③ 行内公式 `$a_t$` 不渲染 ④ 单字母 `$g$` 转换后文字排列错乱 |
| **决策方向** | 命令在 chat_fn 中拦截直接返回、状态栏通过 chatbot.change 事件实时刷新、read_pdf 前自动去重同名论文、新增 delete_paper 工具、行内公式正则改为仅转换含 LaTeX 命令/下标的 |
| **交付物** | `web_ui.py`（命令拦截 + 状态刷新 + 智能公式正则）、`graph_builder.py`（delete_paper 工具 + 去重）、`requirements.txt`（gradio>=5.0） |
| **状态** | ✅ 已交付 |

### 技术要点

**Web UI 命令系统**
- `/help` `/model` `/tokens` `/indexed` `/new` 在 `chat_fn` 中拦截，直接返回 Markdown
- 状态栏通过 `chatbot.change` 事件在每次对话后自动刷新，不再锁定为零
- Gradio 6.0 API 适配：theme/css 移至 `launch()`、移除 `retry_btn`/`undo_btn`/`clear_btn`/`every`

**论文清理**
- `delete_paper` 工具：通过标题或 paper_id 删除 RAG 论文库中的索引
- 自动去重：`read_pdf` 索引前检测同名论文 → 先删旧再存新

**公式渲染修复**
- 初版正则会转换所有 `$...$`，导致单字母 `$g$` 被转为 `$$g$$` 后文字排列错乱
- 修复：仅转换含 `\`（LaTeX 命令）或 `_`/`^`（下标/上标）的公式
- 正则：`(?<!\$)\$([^\$]*[\\_^][^\$]*)\$(?!\$)` → `$$\1$$`

### Gradio 版本兼容

| 问题 | 解决 |
|------|------|
| `gr.Blocks(theme=, css=)` 报 warning | 移至 `launch(theme=, css=)` |
| `retry_btn=, undo_btn=, clear_btn=` 报错 | Gradio 6 已移除这些参数 |
| `demo.load(every=5)` 报错 | 改为 `chatbot.change` 事件刷新 |

---

## 条目 010 — 笔记功能

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-20 |
| **需求** | 新增科研笔记功能：按研究方向分话题、支持长文本、Web UI 弹出面板 |
| **决策方向** | SQLite 持久化（notes.db），按话题分组，Web UI 用 Gradio Accordion 做可折叠面板，`/note` 命令系交互 |
| **交付物** | `notes.py`（NoteStore SQLite 后端）、`web_ui.py`（笔记面板 + /note 命令）、`.gitignore`（排除 notes.db） |
| **状态** | ✅ 已交付 |

### 技术要点

**数据模型**（SQLite）
- `topics(id, name, created_at)` — 研究方向
- `notes(id, topic_id, content, created_at)` — 笔记内容
- 话题名唯一约束、笔记外键关联话题

**交互方式**
- 聊天框输入 `/note` 命令：`topics` / `topic <名>` / `add <内容>` / `list` / `del <编号>`
- Web UI 折叠面板（Accordion "📝 笔记"）：话题下拉框 + 文本区 + 保存按钮
- 面板支持自定义话题（`allow_custom_value=True`）

**不涉及**
- Agent 工具 — 纯 UI 层，不走 LLM
- 终端版 — 本期只做 Web UI

**零新依赖** — 仅用 Python 标准库 `sqlite3`

---

## 条目 011 — PDF 图片提取 + 多模态看图

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-20 |
| **需求** | pdfplumber 无法提取论文图片；尝试 Marker 安装因 Python 3.14 + HF 网络受限而放弃 |
| **决策方向** | 放弃 Marker，改用 PyMuPDF 提取嵌入图片 + GLM-4V API 看图描述（用户已申请智谱 Key） |
| **交付物** | `pdf_reader.py`（extract_images 函数替代 MarkerReader）、`graph_builder.py`（read_pdf 后自动提图 + describe_image 工具）、`requirements.txt`（新增 PyMuPDF，删除 marker-pdf） |
| **状态** | ✅ 已交付 |

### 技术要点

- PyMuPDF `fitz.open().get_images()` → 提取嵌入图片到 `data/papers/images/`
- `read_pdf` 返回结果自动追加图片路径列表
- `describe_image` 工具：本地 base64 编码 → GLM-4V `glm-4v` 模型看图 → 中文描述
- 路径 fallback：自动尝试 `data/papers/` 和 `data/papers/images/`

### Marker 安装排障记录

| 问题 | 原因 | 最终 |
|------|------|------|
| Pillow 10.4.0 无 py314 wheel | Python 3.14 太新 | 用已有 Pillow 12.2.0 |
| tokenizers 版本冲突 | transformers 锁死 ≤0.23.0 | 降级至 0.22.2 |
| OCR error 模型 401 | 镜像不完整 + 子进程不继承代理 | 放弃 Marker |
| **结论**：Python 3.14 + Windows + 网络受限三重不利，Marker 不适合当前环境 |

---

## 条目 012 — Prompt Engineering 系统级升级

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-20 |
| **需求** | 三角度分析指出的"最该学系统级 Prompt Engineering"：少样本、深度纠错、token 预算分配 |
| **决策方向** | 三连改：① 系统提示词加 2 个 Few-shot 范例 ② verify 节点加智能上下文压缩（read_pdf 后替换原文） ③ verify 节点加跨论文库自检（query_papers 注入验证） ④ 启动时从 checkpoint 估算上下文占比 |
| **交付物** | `graph_builder.py`（verify_node 重写 + _SYSTEM_PROMPT 新增范例）、`research_agent.py`（_load_context_from_checkpoint 启动估算） |
| **状态** | ✅ 已交付 |

### 技术要点

**智能上下文压缩**
- 检测：read_pdf 后 LLM 生成了摘要卡片（含"摘要"关键词）
- 动作：将原始 PDF 文本（3000+ 字符）替换为一行引用
- 效果：读 3 篇论文回收 ~30K tokens

**少样本范例**
- 范例1：精读论文后的摘要卡片格式
- 范例2：回答论文细节时的 `[已读·Table 2]` 来源标签格式
- 代价：永久占 ~400 tokens，零运行时开销

**跨论文库自检**
- verify 节点检测 read_pdf → `query_papers` 检索 2 条最相关内容
- 注入验证 prompt，LLM 交叉对比数据和来源
- 每次 verify 多 1 次 API 调用（~200 tokens）

**上下文估算修复**
- 启动时从 `checkpoint.db` 读取已有消息
- 总字符数 / 2 ≈ 估算 tokens
- `/new` 时清零，状态栏实时显示准确占比

---

## 条目 013 — 三角度分析（条目 012 截止）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-20 |
| **类型** | 阶段性复盘，非功能需求 |
| **状态** | 📋 已记录 |

### 三角度结论

| 视角 | 结论 |
|------|------|
| **用户最缺** | 主动研究流程 — 定时搜索、自动精读、简报推送。目前所有行为是被动响应的，Agent 不会在没有用户命令时做任何事 |
| **你该精进** | 前端交互设计 — 流式输出（SSE）、多 Tab 布局、论文卡片组件、配置文件管理。后端技术已打得够深，边际收益递减 |
| **最大问题** | 零测试 — 5 个模块互相依赖，改一行要手动启动 Web UI 验证全链路。3 个关键测试即可显著改善：read_pdf 链路、query_papers 命中率、verify 节点边界不崩溃 |

### 后续方向（按优先级）

| 优先级 | 方向 | 改动量 | 说明 |
|--------|------|--------|------|
| P0 | 3 个核心测试 | ~40 行 | 重构安全网 |
| P1 | 流式输出 | ~20 行 | 体验质变 |
| P2 | 多 Tab 布局 | ~60 行 | 聊天/论文库/笔记分离 |
| P3 | 主动研究流程 | 新增模块 | 后台定时任务 + 简报生成 |

---

---

## 条目 014 — 用户画像 + 双记忆架构

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-20 |
| **需求** | 给记忆分两层：短期（会话上下文）、长期（用户画像） |
| **决策方向** | 短期 = checkpoint.db 最近 50 快照自动剪裁；长期 = profile.md（Markdown，人 + Agent 共维护）；Agent 通过 update_profile 工具自动学习用户偏好 |
| **交付物** | `profile.py`（ProfileManager）、`graph_builder.py`（update_profile 工具 + 画像注入系统提示词）、`research_agent.py`（profile 初始化 + checkpoint 剪裁）、`web_ui.py`（/profile 命令） |
| **状态** | ✅ 已交付 |

### 技术要点

**profile.md 结构**（Markdown，Git 版本控制友好）
- 研究方向、偏好设置、活跃问题、已读论文
- Agent 通过 `update_profile` 工具自动追加
- 用户可手动编辑

**Agent 自动学习**
- 发现新研究方向 → `update_profile("add_direction", "xxx")`
- 发现新偏好 → `update_profile("set_preference", "模型: xxx")`
- 读完论文 → `update_profile("add_paper", "标题: 一句话总结")`
- 每轮对话 system prompt 注入画像摘要

**checkpoint 剪裁**
- 启动时扫描 checkpoint.db，每个 thread 只保留最近 50 个快照
- `/new` 开新 thread，旧 thread 快照仍在但会被剪裁

---

## 条目 015 — 多 Tab + 论文卡片 + 配置管理 + 测试

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-21 |
| **需求** | 条目 013 P2 方向：多 Tab 布局、论文卡片组件、配置文件管理、3 个核心测试、verify 修复 |
| **决策方向** | Gradio Tabs 四页（对话/论文库/笔记/设置）；论文卡片用 gr.HTML 渲染；config.yaml 统一非敏感设置；unittest 覆盖 read_pdf/query_papers/verify 三条链路 |
| **交付物** | `web_ui.py`（Tab 重构）、`config.yaml`（统一配置）、`tests/test_core.py`（11 个测试全部通过）、`graph_builder.py`（verify 反馈改 metadata + 公式判宽） |
| **状态** | ✅ 已交付 |

### 技术要点

**四 Tab 布局**
- 💬 对话 — 聊天界面（流式、命令）
- 📚 论文库 — 卡片式列表 + 刷新按钮
- 📝 笔记 — 从折叠面板移入独立标签页
- ⚙ 设置 — 模型选择 + RAG 开关 + PDF 页数，保存到 config.yaml

**配置文件** — config.yaml（YAML）+ PyYAML
- 非敏感设置集中管理（model、pdf.max_pages、rag.enabled、ui.port）
- API Key 仍保留在 .env（安全性）
- Web UI 设置面板可直接修改并持久化

**核心测试** — tests/test_core.py（11 tests, ~4s）
- read_pdf 链路：提取 → 分块 → 索引 → 去重
- query_papers 链路：索引 → 查询 → 命中 → 距离 → 列表 → 删除
- verify 边界：无工具跳过 / 空回复不崩溃 / 图编译 / 全模块导入

**verify 修复**
- 反馈从 `messages(role:system)` 改为 `metadata.verify_feedback` → 聊天框不可见
- 公式检查加宽松说明 → 不过度标记 LaTeX 格式公式
- 工具结果截断 600→2000 → 公式不被拦腰切断

---

## 条目 016 — verify 反馈隐藏 + 公式检查放宽

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-21 |
| **需求** | verify 反馈显示在聊天框；解读公式时 verify 反复拦截 |
| **决策方向** | 反馈改走 metadata（用户不可见）；工具结果截断 600→2000；公式判宽（query_papers 返回的 LaTeX 不严格比对） |
| **交付物** | `graph_builder.py`（verify_node + verify_router + llm_node 三处联动） |
| **状态** | ✅ 已交付 |

---

## 条目 017 — 拆分 + 日志 + 降级 + 配置统一

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-21 |
| **需求** | graph_builder.py 930 行单文件难维护；缺少结构化日志；ChromaDB 故障时崩溃；配置散落四处 |
| **决策方向** | 拆分 graph_builder → prompts/tool_schemas/tools/；logger.py 统一日志 + --debug；NoOpStore 降级后备；config.py Config 类合并 .env/yaml/env/CLI |
| **交付物** | `prompts.py` `tool_schemas.py` `tools/` `logger.py` `config.py` `search_api.py`、瘦身 `graph_builder.py`（280行） |
| **状态** | ✅ 已交付 |

---

## 条目 018 — 多模态文件输入 + 校验 + README

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-21 |
| **需求** | 用户只能输入文字，需要上传 PDF/图片直接读取/看图；README 过时 |
| **决策方向** | gr.MultimodalTextbox 📎 按钮 + 拖拽覆盖层；PDF→自动注入 read_pdf，图片→describe_image；三层校验（大小100MB/同名/同内容MD5）；README 重写为 16 条目后的真实状态 |
| **交付物** | `web_ui.py`（文件输入+校验+拖拽层）、`README.md`（重写） |
| **状态** | ✅ 已交付 |

---

---

## 条目 019 — 三角度分析（条目 018 截止）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-21 |
| **类型** | 阶段性复盘 |
| **状态** | 📋 已记录 |

### 一、面试官视角 — 不录用的理由

"项目看起来能跑，但放不进团队。"

| 问题 | 面试官怎么看 |
|------|------------|
| 无英文文档 | 需求决策日志、重构说明全中文，评估不了决策过程 |
| 无 CI/CD | 11 个测试写好了但没人跑 |
| 路径硬编码残留 | config 统一了大部分但没全清 |
| Web UI 无认证 | 局域网内任何人都能操作 |

最致命：README 和代码对不上——看 README 以为是 demo，看代码发现是 16 次迭代的真实系统。

### 二、学习方向

| 该学 | 落点 |
|------|------|
| GitHub Actions | 每次 push 自动跑测试 |
| Prompts 版本管理 | 提示词改过 20 次，无 A/B 记录 |
| Docker 化 | 别人没法 5 分钟跑起来 |

不是学新框架——是把已有东西产品化。

### 三、最大问题

verify 重试时用户完全不知道在干什么——加两行状态提示即可消除最大困惑点。

---

---

## 条目 020 — 记忆模块（三元组 + 对话摘要）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-21 |
| **需求** | 提升记忆系统：对话摘要、轻量论文关系索引 |
| **决策方向** | memory.db（SQLite）存三元组（论文→方法/结果）和对话摘要；verify_node 后自动生成摘要；llm_node 注入摘要；read_pdf 后提取三元组 |
| **交付物** | `memory.py`（MemoryStore）、`graph_builder.py`（摘要触发 + 注入）、`tools/read_pdf.py`（三元组提取）、`tool_schemas.py`（memory_search） |
| **状态** | ✅ 已交付 |

---

## 条目 021 — 每日论文自动检索

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-22 |
| **需求** | 每日自动检索最新论文，用户自定义关键词，带查询时间管理避免重复 |
| **决策方向** | scheduler.py + daily.db 管理关键词和时间戳；启动时后台异步搜索；支持 /daily 系列命令；设置 Tab 管理关键词；断点恢复 + 事务保护 |
| **交付物** | `scheduler.py`（Scheduler）、`web_ui.py`（启动推送 + 命令）、`config.yaml`（daily_search 配置段） |
| **状态** | ✅ 已交付 |

---

## 条目 022 — 公式/表格语义保护 + CI + 多源搜索

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-22 |
| **需求** | chunk 切分可能打断公式；表格跨页丢失连续性；GitHub Actions CI；Semantic Scholar 限流严重 |
| **决策方向** | chunk_text 预扫描 $$...$$ 标记不可切割区；pdf_reader 用 find_tables() 坐标判断跨页合并；GitHub Actions 自动跑 11 个测试；三源全量搜索（SS + arXiv + OpenAlex）合并去重 |
| **交付物** | `paper_store.py`（公式感知 chunk）、`pdf_reader.py`（跨页表格）、`.github/workflows/test.yml`（CI）、`scheduler.py`（三源全量） |
| **状态** | ✅ 已交付 |

---

---

## 条目 023 — 分层切块 + 图片提取重构 + verify 分级

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-22 |
| **需求** | RAG 切块无章节语义；图片提取破坏子图关系；verify 误报频繁 |
| **决策方向** | ① 章节感知切块（## / ### 两级 + 前缀注入 + 句子边界 + 二分查找 + 死循环修复）② 图注感知图片提取（get_image_info 坐标 + 图注锚点 + 外接矩形渲染 + 缓存 + 描述缓存 + 图注 sidecar）③ verify 分级（严重重生成/轻微仅提示 + 跳过门控 + 相关性过滤 + 增量验证 + 重试历史 + 降级静默） |
| **交付物** | `paper_store.py`（chunk_text 重写）、`pdf_reader.py`（extract_images 重写）、`graph_builder.py`（verify_node 重写）、`memory.py`（描述缓存表）、`tools/describe.py`（缓存+图注）、`tools/search.py`（delete_paper 联动删图） |
| **状态** | ✅ 已交付 |

### 关键修复
- chunk 死循环：巨大公式区间把切点拨回 start 前 → 强制 end 不后退
- 1720 个 1 字符 chunk → 19 个正常 chunk

---

## 条目 024 — Token 消耗优化

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-22 |
| **需求** | token 消耗大头在工具结果和 verify 输入 |
| **决策方向** | ① 工具结果按类型差异化截断（query 5000/search 3000/read_pdf 12000）② verify 只送含事实的工具结果 ③ 预算预警前置（60%/90%）④ 缓存前缀稳定 ⑤ 摘要 10 分钟频率控制 |
| **交付物** | `graph_builder.py`（tool_node 差异化截断 + verify 输入精简 + 摘要频率）、`memory.py`（get_last_summary_time）、`web_ui.py`（budget_warning 显示） |
| **状态** | ✅ 已交付 |

---

## 条目 025 — 稳定性修复与发布前回归

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-03 |
| **需求** | 代码审查后修复 CLI、Web UI、验证状态与每日检索的可用性问题，并补齐回归测试。 |
| **问题** | ① CLI 启动引用未定义模型变量，`/model-set` 使用过期构造参数；② Web 文件冲突确认未读取用户回复；③ 后台每日检索在线程启动后才初始化队列，`/daily search` 和 `/daily retry` 仅提示不执行；④ verify 重试反馈残留至后续会话；⑤ 缺失 ChromaDB 时 `NoOpStore` 无法导入。 |
| **决策方向** | CLI 统一从 `Config` 读取模型并用 dataclass `replace()` 切换；Web UI 在线程启动前初始化队列、保留上传原始说明、支持临时检索和当日重试；验证完成后清理仅本轮有效的 metadata；Chroma 导入与降级逻辑置于同一保护分支。 |
| **交付物** | `research_agent.py`、`web_ui.py`、`graph_builder.py`、`scheduler.py`、`paper_store.py`、`tests/test_core.py`。 |
| **验证** | `python tests/test_core.py`：13/13 通过；新增验证状态清理和每日检索重试/临时检索的无外网测试。 |
| **状态** | ✅ 已交付 |

---

## 条目 026 — 本地多会话隔离

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-03 |
| **需求** | 本地 Docker 化前先完成可展示的多会话：可创建、切换、删除，且历史、摘要与流式输出不能串线。 |
| **决策方向** | 会话目录和 LangGraph checkpoint 共用 `checkpoint.db`；每次请求携带 `session_id`，同会话通过互斥锁串行，不同会话可并行；流式回调和 Token 统计改为请求/会话级，不再使用全局回调。 |
| **兼容策略** | 发现旧版 `research-main` checkpoint 时自动注册为“历史会话”；CLI `/new` 保持可用，Web UI 改用会话栏操作。 |
| **删除语义** | 勾选确认后硬删除会话元数据、Token 统计和所有带 `thread_id` 的 LangGraph checkpoint 行；同步清理该会话摘要，不删除全局论文库、笔记、PDF、三元组和图片缓存。 |
| **交付物** | `session_store.py`、`research_agent.py`、`graph_builder.py`、`memory.py`、`web_ui.py`、`tests/test_core.py`。 |
| **验证** | `python tests/test_core.py`：15/15 通过；覆盖旧会话迁移、自动命名、会话删除隔离和按 session_id 读取摘要。 |
| **状态** | ✅ 已交付 |

---

## 条目 027 — 长任务后台化与验证熔断

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-03 |
| **问题** | 每日检索按关键词和数据源顺序执行，单源 30 秒超时会让任务持续数分钟；`verify` 在主 LangGraph 链路中同步等待 30 秒，DeepSeek 波动时用户会感到页面卡住。 |
| **决策方向** | 每日任务转入独立后台线程，查询结果在定时刷新的任务面板展示，不写入聊天记录；Semantic Scholar、arXiv、OpenAlex 使用有界线程池并行查询，每源 8 秒读超时。 |
| **验证降级** | verify 使用 3.05 秒连接 + 8 秒读取超时；连续失败两次后熔断 120 秒并直接返回主回答，验证恢复后自动闭合。对话摘要也使用同一短超时。 |
| **配置** | `config.yaml` 的 `daily_search.request_timeout_seconds` 和 `agent.verify_timeout_seconds` 可调整，最低为 3 秒。 |
| **验证** | 新增三源短超时与 verify 熔断测试；`python tests/test_core.py`：17/17 通过。 |
| **状态** | ✅ 已交付 |

---

## 条目 028 — 主模型请求韧性（不降级模型）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-03 |
| **约束** | 主模型失败时不允许自动切换到其他模型，避免质量、成本和回答风格在用户不知情时变化。 |
| **问题** | 短时并发请求会直接压向 API；429、5xx、连接/读取超时只会立即报错；流式响应中断后没有恢复路径。 |
| **决策方向** | 新增共享 `LLMClient`：全局有界并发（默认 4）、有界等待队列（默认 20）、同模型最多 2 次指数退避重试、45 秒请求截止时间、连续 3 次失败熔断 120 秒。 |
| **流式语义** | 未收到 token 前允许安全重放同一请求；已输出 token 后保留现有文本且不自动重放，以免重复回答。Web 提供 `/retry` 重发本进程最后一个模型请求。 |
| **配置** | `api_resilience` 可配置并发、队列、连接/读取超时、截止时间、重试和熔断；不包含 fallback model 配置。 |
| **验证** | 新增同模型重试、熔断拒绝请求和首 token 前流式重连测试；`python tests/test_core.py`：20/20 通过。 |
| **状态** | ✅ 已交付 |

---

## 条目 029 — 请求端到端预算与半开熔断

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-03 |
| **问题** | 首版 45 秒截止时间未包含排队和 SSE 正文；流式 HTTP 200 在正文完成前就清除失败计数；熔断恢复后会允许所有并发请求同时探测下游。 |
| **决策方向** | 每次模型调用创建共享 `RequestBudget`，覆盖排队、HTTP 重试、SSE 正文和首 token 前重放，最多两次重试总额不变；流式连接持有并发槽直到 `[DONE]`；熔断器采用 closed/open/half_open，恢复期只放行一个探测请求。 |
| **约束保持** | 仍只请求配置中的主模型，不新增 fallback model 或任何自动降级路径。 |
| **验证** | 新增排队截止时间、流式槽位/成功确认、半开单探测测试；核心测试总数更新为 23。 |
| **状态** | ✅ 已交付 |

---

## 条目 030 — 统一运行时数据边界

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-05 |
| **问题** | SQLite、Chroma、PDF、图片和画像分散在项目根目录与 `data/` 下；Web UI 会直接改写 `config.yaml`，容器化后难以只挂载数据卷，也容易把用户状态混入代码目录。 |
| **决策方向** | 新增版本化 `RuntimePaths`，以 `APP_DATA_DIR`（默认 `runtime/`）统一所有可变路径。原始数据放在 `primary/`，可重建数据放在 `derived/`，状态版本放在 `meta/state.json`。 |
| **设置策略** | 静态默认值仍在 `config.yaml`；密钥留在 `.env`；Web UI 用户设置写入 `runtime/primary/settings.json`，优先级低于环境变量和 CLI。 |
| **迁移与备份** | 不自动搬迁旧数据；`migrate_runtime.py` 默认预览、`--apply` 才复制，且不删除源文件。`backup_runtime.py` 用 SQLite online backup 生成可移植 ZIP。 |
| **验证** | 新增运行时目录、用户设置覆盖、路径约束、默认预览且无损复制迁移、SQLite 在线 ZIP 备份回归测试；核心回归测试总数更新为 26。 |
| **状态** | ✅ 已交付 |

---

*后续条目将以此格式持续追加。*
## 条目 031：Docker Compose 部署与运行数据持久化

- **背景**：本地开发需要可复现的一键启动方式，且 Docker 容器重建不能丢失多会话、论文和设置。
- **决策**：采用单服务 Docker Compose；API Key 仅由未提交的 `.env` 注入，`runtime/` 作为唯一 bind mount，容器内部固定为 `/app/runtime`。
- **不采用**：不在镜像内复制 `.env` 或 `runtime/`；不引入数据库、消息队列等额外服务，避免当前单机项目的部署复杂度超过收益。
- **结果**：`docker compose up --build -d` 可启动 Web UI；代码升级只需重建容器，用户数据仍在宿主机 `runtime/`。
- **补充**：基础镜像通过 `PYTHON_IMAGE` 构建参数覆盖，适配 Docker Hub 受限、校园网或企业镜像仓库环境；默认仍使用官方 `python:3.11-slim`。
## 条目 032：新建会话的事件回路与隔离修复

- **问题**：新建会话后偶发显示旧历史，并使页面处于加载状态。
- **根因**：Gradio 的 `Chatbot.change` 同时响应用户输入与程序化赋值；它把旧状态回写给会话选择器，触发了级联切换。
- **决策**：单向数据流：会话操作负责变更会话和历史，聊天框事件只更新状态栏；请求必须携带当前浏览器的有效会话 ID。
- **结果**：新会话从空历史开始，无法再因状态乱序回退到其他会话。
