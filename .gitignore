# 环境变量（含 API Key）
.env

# 运行时生成
__pycache__/
runtime/
backups/
evals/reports/*.json

# Secrets stay on the host and are injected by Docker Compose at runtime.
.env
.env.*
!.env.example
# 旧版分散路径（保留忽略以支持显式迁移）
chroma_data/
checkpoint.db
checkpoint.db-*
notes.db
notes.db-*
memory.db
memory.db-*
daily.db
daily.db-*

# 下载的论文 PDF
data/papers/
